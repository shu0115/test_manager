
<h1><%= t("user.list") %></h1>

<table>
  <tr>
    <th><%= t("user.name") %></th>
    <th><%= t("user.created_at") %></th>
  </tr>

<% @users.each{ |user| %>
  <tr>
    <td><%= user.name %></td>
    <td><%= user.created_at.strftime("%Y/%m/%d %H:%M") unless user.created_at.blank? %></td>
  </tr>
<% } %>
</table>

<br />
